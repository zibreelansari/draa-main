import { FormEvent, useState } from "react";
import { ArrowRight, Building2, CheckCircle2, GraduationCap } from "lucide-react";
import { Link, useParams } from "react-router-dom";
import { apiRequest } from "../lib/api";

export default function RegisterPage(){const{role="student"}=useParams();const institute=role==="institute";const[message,setMessage]=useState("");const[error,setError]=useState("");const[busy,setBusy]=useState(false);async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();setBusy(true);setError("");const form=new FormData(event.currentTarget);const body=institute?{instituteName:form.get("instituteName"),contactName:form.get("contactName"),email:form.get("email"),password:form.get("password"),city:form.get("city"),website:form.get("website")}:{firstName:form.get("firstName"),lastName:form.get("lastName"),email:form.get("email"),password:form.get("password"),country:form.get("country")};try{const result=await apiRequest<{message:string}>(`/api/auth/register/${institute?"institute":"student"}`,{method:"POST",body:JSON.stringify(body)});setMessage(result.message);event.currentTarget.reset()}catch(err){setError(err instanceof Error?err.message:"Registration failed.")}finally{setBusy(false)}}const Icon=institute?Building2:GraduationCap;return <section className="registration-page"><div className="portal-shell registration-grid"><div><span>CREATE AN ACCOUNT</span><h1>{institute?"Institute Registration":"Student Registration"}</h1><p>{institute?"Submit your institution profile for DRAA administrator review.":"Create your student profile to explore courses and manage applications."}</p><ul><li><CheckCircle2 size={16}/>Secure role-based account</li><li><CheckCircle2 size={16}/>Application and status tracking</li><li><CheckCircle2 size={16}/>Clear privacy and access controls</li></ul></div><form onSubmit={submit}><Icon size={27}/>{institute?<><label>Institute name<input name="instituteName" required minLength={3}/></label><label>Primary contact<input name="contactName" required minLength={2}/></label><label>City<input name="city" required minLength={2}/></label><label>Website<input name="website" type="url" placeholder="https://"/></label></>:<><div className="form-row"><label>First name<input name="firstName" required minLength={2}/></label><label>Last name<input name="lastName" required minLength={2}/></label></div><label>Country<input name="country" required minLength={2}/></label></>}<label>Email address<input name="email" type="email" required/></label><label>Password<input name="password" type="password" required minLength={10}/><small>Use at least 10 characters.</small></label>{error&&<p className="form-error">{error}</p>}{message&&<p className="form-success">{message}</p>}<button disabled={busy}>{busy?"Submitting…":"Create account"}<ArrowRight size={16}/></button><p>Already registered? <Link to={`/login/${role}`}>Log in</Link></p></form></div></section>}
