{
  "name": "draa-study-india-platform",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "build:shared": "npm run build -w @draa/shared",
    "build:database": "npm run build -w @draa/database",
    "build:api": "npm run build -w @draa/api",
    "build:web": "npm run build -w @draa/web",
    "build:packages": "npm run build:shared && npm run build:database",
    "build": "npm run build:packages && npm run build:api && npm run build:web",
    "dev": "npm run build:packages && concurrently -n API,WEB -c blue,green \"npm run dev -w @draa/api\" \"npm run dev -w @draa/web\"",
    "seed": "npm run seed -w @draa/api",
    "start": "npm run start -w @draa/api"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "typescript": "~5.6.2"
  }
}
