export { USER_ROLES } from '../types/user';
export type { UserRole, UserStatus } from '../types/user';

export { COURSE_LEVELS, COURSE_MODES, COURSE_TYPES } from '../types/course';
export type { CourseLevel, CourseMode, CourseType } from '../types/course';

export { PUBLISH_STATUSES, APPROVAL_STATUSES } from '../types/course';
export type { PublishStatus, ApprovalStatus } from '../types/course';

export { APPLICATION_STATUSES, DOCUMENT_STATUSES, NOTIFICATION_KINDS, TICKET_STATUSES, TICKET_PRIORITIES } from '../types/application';
export type { ApplicationStatus, DocumentStatus, NotificationKind, TicketStatus, TicketPriority } from '../types/application';
